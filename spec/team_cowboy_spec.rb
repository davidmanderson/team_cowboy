require "spec_helper"

describe TeamCowboy do
  it "has a version number" do
    expect(TeamCowboy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
